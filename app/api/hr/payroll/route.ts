import{NextResponse}from"next/server";import{prisma}from"@/lib/prisma";import{createPayrollRun,hrErrorResponse}from"@/lib/hr";
export async function GET(){return NextResponse.json(await prisma.payrollRun.findMany({include:{lines:{include:{employee:true,payment:true}},journalEntry:{include:{lines:true}}},orderBy:[{year:"desc"},{month:"desc"}]}))}export async function POST(request:Request){try{const body=await request.json(),row=await prisma.$transaction(tx=>createPayrollRun(tx,body));return NextResponse.json(row,{status:201})}catch(e){console.error(e);const r=hrErrorResponse(e);return NextResponse.json({error:r.message},{status:r.status})}}
